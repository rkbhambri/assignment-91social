import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="header d-flex justify-content-between">
            <p>91 Social</p>
        </div>
    );
};

export default Header;
